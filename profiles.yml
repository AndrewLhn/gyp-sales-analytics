gyp_sales_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      pass: postgres
      port: 5432
      dbname: gyp_sales
      schema: raw
      threads: 4
